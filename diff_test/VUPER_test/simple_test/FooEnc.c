/**
 * This file was generated by the Objective Systems ASN1C Compiler
 * (http://www.obj-sys.com).  Version: 7.9.0, Date: 31-May-2025.
 *
 * Command:  asn1c test.asn1 -c -jer -uper -genMake
 */
#include "Foo.h"
#include "rtxsrc/rtxCharStr.h"
#include "rtxsrc/rtxIntEncode.h"

EXTERN int asn1PE_S1 (OSCTXT* pctxt, S1* pvalue)
{
   int stat = 0;
   OSUINT32 pos;
   void* pPerField;
   OSBOOL extbit = FALSE;

   RTXCTXTPUSHTYPENAME (pctxt, "S1");

   extbit = (OSBOOL)(pvalue->t > 1);

   /* extension bit */

   stat = rtxEncBit (pctxt, extbit);
   if (stat != 0) return LOG_RTERR (pctxt, stat);

   if (!extbit) {

      /* Encode choice index value */

      RTXCTXTPUSHELEMNAME (pctxt, "t");

      RTXCTXTPOPELEMNAME (pctxt);

      /* Encode root element data value */

      switch (pvalue->t)
      {
         /* b1 */
         case 1:
            RTXCTXTPUSHELEMNAME (pctxt, "b1");

            stat = rtxEncBit (pctxt, pvalue->u.b1);
            if (stat != 0) return LOG_RTERR (pctxt, stat);

            RTXCTXTPOPELEMNAME (pctxt);
            break;

         default:
            return LOG_RTERR (pctxt, RTERR_INVOPT);
      }
   }
   else {
      /* Encode extension choice index value */

      stat = pe_SmallNonNegWholeNumber (pctxt, pvalue->t - 2);
      if (stat != 0) return LOG_RTERR (pctxt, stat);

      /* Encode extension element data value */

      stat = pe_OpenTypeStart (pctxt, &pos, &pPerField);
      if (stat != 0) return LOG_RTERR (pctxt, stat);

      switch (pvalue->t)
      {
         /* b2 */
         case 2:
            stat = rtxEncBit (pctxt, pvalue->u.b2);
            if (stat != 0) return LOG_RTERR (pctxt, stat);
            break;

         /* b3 */
         case 3:
            stat = rtxEncBit (pctxt, pvalue->u.b3);
            if (stat != 0) return LOG_RTERR (pctxt, stat);
            break;

         default:
            stat = rtxEncBitsFromByteArray (pctxt, pvalue->u.extElem1->data, pvalue->u.extElem1->numocts * 8);
            if (stat != 0) return LOG_RTERR (pctxt, stat);
      }

      stat = pe_OpenTypeEnd (pctxt, pos, pPerField);
      if (stat != 0) return LOG_RTERR (pctxt, stat);
   }

   RTXCTXTPOPTYPENAME (pctxt);

   return (stat);
}

EXTERN int asn1JsonEnc_S1 (OSCTXT *pctxt, S1 *pvalue)
{
   int stat = 0;

   OSRTSAFEPUTCHAR_ZTERM (pctxt, '{');
   rtJsonEncIncrIndent(pctxt);
   rtJsonEncIndent (pctxt);
   switch(pvalue->t) {
      case 1 : 
      {
         OSRTSAFEPUTCHAR_ZTERM (pctxt, '"');
         rtxCopyAsciiText (pctxt, "b1");
         OSRTSAFEZTERM(pctxt);
         rtxCopyAsciiText (pctxt, "\":");
         OSRTSAFEZTERM(pctxt);
         stat = rtJsonEncBoolValue (pctxt, pvalue->u.b1);
         break;
      }

      case 2 : 
      {
         OSRTSAFEPUTCHAR_ZTERM (pctxt, '"');
         rtxCopyAsciiText (pctxt, "b2");
         OSRTSAFEZTERM(pctxt);
         rtxCopyAsciiText (pctxt, "\":");
         OSRTSAFEZTERM(pctxt);
         stat = rtJsonEncBoolValue (pctxt, pvalue->u.b2);
         break;
      }

      case 3 : 
      {
         OSRTSAFEPUTCHAR_ZTERM (pctxt, '"');
         rtxCopyAsciiText (pctxt, "b3");
         OSRTSAFEZTERM(pctxt);
         rtxCopyAsciiText (pctxt, "\":");
         OSRTSAFEZTERM(pctxt);
         stat = rtJsonEncBoolValue (pctxt, pvalue->u.b3);
         break;
      }

      case 4 : 
      {
         /* encode choice extension */
         stat = rtJsonAsn1EncExtElem (pctxt, pvalue->u.extElem1);
         if (stat != 0) return LOG_RTERR (pctxt, stat);
         break;
      }

      default:  {
         return LOG_RTERR (pctxt, RTERR_INVOPT);
      }
   }
   rtJsonEncDecrIndent(pctxt);
   rtJsonEncIndent (pctxt);
   OSRTSAFEPUTCHAR_ZTERM (pctxt, '}');

   return stat;
}

